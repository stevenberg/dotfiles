#!/usr/bin/env ruby
# frozen_string_literal: true

def system!(*args)
  system(*args) || abort("\n== Command #{args} failed ==")
end

abort "usage: git bugfix NEW_BRANCH" if ARGV.empty?

system!("git feature bugfix-#{ARGV.first}")
